modules:

  - name: module
    buildsystem: simple
    build-commands:
      - echo 'echo hello_world' > "${FLATPAK_DEST}/bin/module"
      - ls -lah /app/bin
      - chmod +x "${FLATPAK_DEST}/bin/module"

  
